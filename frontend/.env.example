# ==============================================================================
# Frontend Environment Configuration - Collaborative Canvas (Vite + React)
# ==============================================================================

# Backend API Base URL (optional - Vite proxy redirects /api and /socket.io in local development)
VITE_API_URL=http://localhost:5000

# Backend WebSocket URL (optional - Vite proxy handles /socket.io automatically)
VITE_WS_URL=http://localhost:5000
